# leanscene-steering-core (free, MIT) — placeholder

Topology placeholder for **Task 02** (levers 1–2). No code yet (Task 00 is
foundation only).

Will hold the tuned tool descriptions (shared with mcp-tools) and the static
`CLAUDE.md` / `.cursorrules` starter that steers the agent away from native broad
sweeps. Agent-side only — no engine mutation (ADR-001). See
`docs/tasks/02-steering.md`.
