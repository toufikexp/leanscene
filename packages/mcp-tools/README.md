# leanscene-mcp-tools (free, MIT) — placeholder

Topology placeholder for **Task 01**. No code yet (Task 00 is foundation only).

Will hold the pinned v1 FastMCP `ToolsetDefinition`s — each with a fixed minimal
schema, a tuned description, and a per-call measurement envelope — built headless
against `FakeGateway`. See `docs/tasks/01-mcp-tools.md`.
