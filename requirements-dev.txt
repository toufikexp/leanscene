# Headless dev + CI dependencies. Offline at test time (tiktoken uses a vendored
# encoding); network is only needed to install these.
-e packages/core
-e packages/unreal-gateway
-e packages/mcp-tools
pytest>=8,<9
